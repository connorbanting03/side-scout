[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -w 4 -b 0.0.0.0:$PORT --timeout 120 --access-logfile - --error-logfile - api:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
